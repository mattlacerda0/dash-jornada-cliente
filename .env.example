# Autenticação (Google OAuth) — projeto interno Quarta Via
AUTH_SUPABASE_URL=https://rckpuebaiswrxzmywllv.supabase.co
AUTH_SUPABASE_ANON_KEY=

# Dados (BASE QV) — somente servidor / Netlify Functions
DATA_SUPABASE_URL=https://<project-ref-base-qv>.supabase.co
DATA_SUPABASE_SERVICE_ROLE_KEY=

# Chatbot "Assistente da Jornada" — webhook do workflow n8n analytics-jornada-chat
N8N_CHAT_WEBHOOK_URL=https://n8n-n8n.orsh7l.easypanel.host/webhook/analytics-jornada-chat

# Token interno servidor-servidor para /api/assistant-data (n8n → portal).
# Gere um segredo forte. NUNCA commitar o valor. Mesmo valor deve estar no n8n como PORTAL_INTERNAL_API_TOKEN.
N8N_INTERNAL_API_TOKEN=

# Variáveis configuradas no n8n (não no portal):
#   PORTAL_API_BASE_URL=https://<dominio-do-portal>
#   PORTAL_INTERNAL_API_TOKEN=<mesmo valor de N8N_INTERNAL_API_TOKEN>

# PORT=4173
